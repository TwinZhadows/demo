spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:32768/backend
    username: postgres
    password: postgrespw
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 5MB

app:
   token:
     secret: mysecret
     issuer: BackEndService

